/*import { modeloGuia } from '../Models/datosGuia.js';
import { modeloDestinatario } from '../Models/destinatario.js';
import { modeloRemitente } from '../Models/remitente.js';
import { modeloUnidades } from '../Models/unidades.js';

export class ServiciosPost {

    async agregarGuia(datos) {
        let datosGuia = new modeloGuia(datos);
        return await datosGuia.save()
    }

    async agregarRemitente(datos) {
        let datosRemitente = new modeloRemitente(datos);
        return await datosRemitente.save()
    }
    async agregarDestinatario(datos) {
        let datosDestinatario = new modeloDestinatario(datos);
        return await datosDestinatario.save()
    }
    async agregarUnidades(datos) {
        let datosUnidades = new modeloUnidades(datos);
        return await datosUnidades.save()
    }
}*/